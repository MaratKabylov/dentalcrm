{
  "name": "dental-saas",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@12.4.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "setup": "corepack pnpm install",
    "build": "corepack pnpm -r run build",
    "dev": "corepack pnpm --filter @dental/contracts run build && corepack pnpm --parallel --stream --filter @dental/api --filter @dental/web --filter @dental/worker run dev",
    "lint": "corepack pnpm -r run lint",
    "test": "corepack pnpm --filter @dental/contracts run build && corepack pnpm -r run test",
    "typecheck": "corepack pnpm --filter @dental/contracts run build && corepack pnpm -r run typecheck",
    "check": "npm run lint && npm run typecheck && npm run test && npm run build",
    "db:generate": "corepack pnpm --filter @dental/api db:generate",
    "db:migrate": "corepack pnpm --filter @dental/api db:migrate",
    "db:seed": "corepack pnpm --filter @dental/api db:seed",
    "infra:up": "docker compose -f infrastructure/docker/compose.yml up -d",
    "infra:down": "docker compose -f infrastructure/docker/compose.yml down"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "globals": "^17.4.0",
    "turbo": "^2.10.13",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.70.0"
  }
}
